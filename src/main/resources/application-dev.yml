spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    #url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/fs}
    #password: ${DBMS_PASSWORD:root}
    #username: ${DBMS_USERNAME:root}
    url: ${DBMS_CONNECTION:jdbc:mysql://mysql-fws-nguyenducviet0682.b.aivencloud.com:10883/defaultdb}
    username: ${DBMS_USERNAME:avnadmin}
    password: ${DBMS_PASSWORD:AVNS_wrpsLhopYsCDqHJPTRV}
    hikari:
      initialization-fail-timeout: 5000
      connection-timeout: 20000
      maximum-pool-size: 5
      minimum-idle: 2
    livereload:
      enabled: false
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: none
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
    add-properties: false
  jmx:
    enabled: false
system:
  login:
    bypass: true